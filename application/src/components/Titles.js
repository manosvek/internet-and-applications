import React from "react";

const Titles = props => (
      <div>
        <h1 className="title-container__title">Trip Planner</h1>
        <p className="title-container__subtitle">Find out weather and route between two cities</p>
      </div>
    );


export default Titles;
